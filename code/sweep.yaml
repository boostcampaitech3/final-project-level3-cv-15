program: train.py

method: bayes

metric:
  name: valid/F1_score
  goal: maximize
  
parameters:
  dir:
    values: ["sensitive"]
  arg_n:
    values: ["arg"]
  # p:
  #   min: 0
  #   max: 1
  # limit:
  #   min: 0
  #   max: 100
  # seed:
  #   values: [42, 24,5]
  lr:
    min: 0.00001
    max: 0.01
  # optimizer:
  #   values: ["adam", "sgd", "adamw"]
  # scheduler:
  #   values: ["lambdalr", "cosineannealinglr", "cosineannealingwarmrestart","exponentiallr","steplr"]
  # loss:
  #   values: ["cross_entropy", "focal", "label_smoothing", "f1"]